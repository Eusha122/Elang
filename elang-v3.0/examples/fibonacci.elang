$$ Eusha Fibonacci Test
fn fib(n) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

say("Fibonacci 0 to 10:").newl
for (i in 0..10) {
    say(fib(i)).space
}
say("").newl
