$$ Scope test: function should NOT mutate global x
x = 5

fn test() {
    x = 10
    say("Inside fn: ").space
    say(x).newl
}

test()
say("Outside fn: ").space
say(x).newl

$$ Closure test: lambda captures defining env
a = 10
f = x => x + a
say("Closure test: ").space
say(f(5)).newl
